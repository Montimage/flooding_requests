name: flooding_get

on:
  push:
   branches: [ main ]
  pull_request:
    branches: [ main ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKER_SECRET }}" | docker login -u "franckccv" --password-stdin
        docker image build -t francesco/flooding_get:1.0 .
        docker push francesco/flooding_get:1.0